name: Package Release

on:
    workflow_dispatch:
    push:
        branches: [main]
        tags: ["v*"]
        paths:
            - "apps/core/**"
            - "apps/electron/**"
            - "packages/extension/**"
            - "scripts/generateLazyPack.ts"
            - "scripts/lazyPackExtensions.ts"
            - ".github/workflows/lazy-package.yml"
            - "package.json"
            - "pnpm-lock.yaml"
            - "pnpm-workspace.yaml"

permissions:
    contents: write

env:
    CSC_IDENTITY_AUTO_DISCOVERY: false

jobs:
    package-linux:
        name: Build Package and Linux Executable
        runs-on: ubuntu-latest

        steps:
            - name: Checkout repository
              uses: actions/checkout@v6

            - name: Install pnpm
              uses: pnpm/action-setup@v5
              with:
                  version: 10
                  run_install: false

            - name: Install Node.js
              uses: actions/setup-node@v6
              with:
                  node-version: 25
                  cache: "pnpm"

            - name: Install dependencies
              run: pnpm install --frozen-lockfile

            - name: Build game package
              run: pnpm generatePackage

            - name: Upload game package artifact
              uses: actions/upload-artifact@v7
              with:
                  name: game-package
                  path: output/lazy-pack
                  retention-days: 30

            - name: Upload import package artifact
              uses: actions/upload-artifact@v7
              with:
                  name: import-package
                  path: output/import-package
                  retention-days: 30

            - name: Build Linux executable
              run: pnpm -F @noname/electron build:linux

            - name: Upload Linux executable artifact
              uses: actions/upload-artifact@v7
              with:
                  name: linux-executable
                  path: output/*.AppImage
                  retention-days: 30

            - name: Package release asset
              if: startsWith(github.ref, 'refs/tags/v')
              run: |
                  cd output/lazy-pack
                  zip -r "$GITHUB_WORKSPACE/noname-package.zip" .
                  cd "$GITHUB_WORKSPACE/output/import-package"
                  zip -r "$GITHUB_WORKSPACE/noname-import-package.zip" .

            - name: Upload release asset
              if: startsWith(github.ref, 'refs/tags/v')
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
              run: |
                  gh release view "$GITHUB_REF_NAME" >/dev/null 2>&1 || gh release create "$GITHUB_REF_NAME" --title "$GITHUB_REF_NAME" --notes "构建产物"
                  gh release upload "$GITHUB_REF_NAME" noname-package.zip noname-import-package.zip output/*.AppImage --clobber

    package-windows:
        name: Build Windows Executable
        needs: package-linux
        runs-on: windows-latest

        steps:
            - name: Checkout repository
              uses: actions/checkout@v6

            - name: Install pnpm
              uses: pnpm/action-setup@v5
              with:
                  version: 10
                  run_install: false

            - name: Install Node.js
              uses: actions/setup-node@v6
              with:
                  node-version: 25
                  cache: "pnpm"

            - name: Install dependencies
              run: pnpm install --frozen-lockfile

            - name: Build game package
              run: pnpm generatePackage

            - name: Build Windows executable
              run: pnpm -F @noname/electron build:win

            - name: Upload Windows executable artifact
              uses: actions/upload-artifact@v7
              with:
                  name: windows-executable
                  path: output/*.exe
                  retention-days: 30

            - name: Upload release asset
              if: startsWith(github.ref, 'refs/tags/v')
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
              shell: pwsh
              run: |
                  foreach ($asset in Get-ChildItem output -Filter "*.exe") {
                      gh release upload "$env:GITHUB_REF_NAME" $asset.FullName --clobber
                  }
